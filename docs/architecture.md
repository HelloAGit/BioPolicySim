# BioPolicySim Architecture

This prototype uses:
- deterministic rule-based parsing
- JSON persona definitions
- a multi-round deliberation engine
- simple fragility metrics

Future extensions include LLM-based extraction, probabilistic dynamics, and API endpoints.
